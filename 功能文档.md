## 功能1：**智能借书与资源调度（Smart Borrowing）**

### 功能描述

用户发起借书请求后，系统通过多智能体协作自动选择最优策略：

- **本地借阅（LOCAL_LOAN）**：目标馆有库存时立即借出
- **跨馆调拨（TRANSFER_PROVIDE）**：目标馆无库存但其他馆有，自动协商调拨并预留24小时
- **预约排队（RESERVATION）**：全馆无库存时创建预约，书归还后自动优先分配

系统内置权限检查（最大借阅数、重复借书限制），确保业务合规。

### 参与智能体

| 智能体              | 角色                 | 职责                                             |
| :------------------ | :------------------- | :----------------------------------------------- |
| UserDemandAgent     | Manager（协调者）    | 接收请求、权限检查、策略选择、调拨执行、预约管理 |
| InventoryAgent-LibN | Contractor（参与者） | 评估本地库存、计算投标评分（距离+压力）、响应CFP |

### 业务流程（时序）

1. **权限检查**：UserDemandAgent检查用户状态、最大借阅数、是否已借该书
2. **策略选择**：
   - **本地优先**：检查`preferredLibraryId`是否有`AVAILABLE`副本 → 直接借书（策略A）
   - **跨馆协商**：本地无库存 → 广播CFP → 收集投标 → 选择最优馆调拨（策略B）
   - **预约兜底**：全馆无库存 → 创建`tb_book_reservation`记录（策略C）
3. **执行与预留**：
   - 策略A：创建借阅记录（`BORROWING`），副本状态改为`BORROWED`
   - 策略B：执行调拨（`tb_book_transfer`），创建借阅记录（`TRANSFERING`），等待书籍调拨完成
   - 策略C：创建预约记录（`PENDING`，3天有效）
4. **结果返回**：返回对应策略的执行结果

### 核心算法

**投标评分公式**（仅策略B使用）：

```plain
score = 80 - (distance(km) × 10) - (pending_reservation_count × 5)
```

### 输入参数

| 字段               | 类型 | 必填 | 说明         | 示例        |
| :----------------- | :--- | :--- | :----------- | :---------- |
| userId             | Long | 是   | 用户ID       | 1           |
| biblioId           | Long | 是   | 书目ID       | 1           |
| preferredLibraryId | Long | 是   | 期望取书馆ID | 1（理科馆） |

**请求示例**：

JSON

```json
{
  "userId": 1,
  "biblioId": 1,
  "preferredLibraryId": 1
}
```

### 输出结果（按策略区分）

**注意**：`strategy`字段标识实际执行的策略，不同策略返回的字段集合不同。

| 字段            | 类型    | 说明                                        | 适用策略                       |
| :-------------- | :------ | :------------------------------------------ | :----------------------------- |
| taskId          | String  | 任务唯一标识                                | 所有                           |
| status          | String  | SUCCESS / FAILED                            | 所有                           |
| strategy        | String  | LOCAL_LOAN / TRANSFER_PROVIDE / RESERVATION | 所有                           |
| message         | String  | 详细提示信息                                | 所有                           |
| copyId          | Long    | 分配的副本ID                                | LOCAL_LOAN / TRANSFER_PROVIDE  |
| libraryId       | Long    | 取书馆ID（本地借阅时）                      | LOCAL_LOAN                     |
| sourceLibraryId | Long    | 来源馆ID（调拨时）                          | TRANSFER_PROVIDE               |
| targetLibraryId | Long    | 目标馆ID（调拨时）                          | TRANSFER_PROVIDE               |
| reserveHours    | Integer | 预留时长（小时）                            | TRANSFER_PROVIDE / RESERVATION |
| reservationId   | Long    | 预约记录ID                                  | RESERVATION                    |
| pickupLibraryId | Long    | 预约取书馆ID                                | RESERVATION                    |

### 不同场景的响应示例

#### 场景A：本地直接借阅（目标馆有库存）

**条件示例**：`preferredLibraryId=1`（理科馆），且理科馆有`AVAILABLE`副本

**响应示例**：

```json
{
  "code": 200,
  "message": "借书成功，请在30天内归还",
  "data": {
    "strategy": "LOCAL_LOAN",
    "borrowId": null,
    "reservationId": null,
    "transferId": null,
    "copyId": 1,
    "sourceLibraryId": null,
    "targetLibraryId": null,
    "message": "借书成功，请在30天内归还",
    "taskId": "TASK-1772194129514-1",
    "estimatedArrivalMinutes": null,
    "status": "SUCCESS"
  }
}
```

#### 场景B：跨馆调拨+预留（目标馆无，其他馆有）

**条件示例**：理科馆0本，文科馆2本

**响应示例**：

```json
{
  "taskId": "TASK-1772004850950",
  "status": "SUCCESS",
  "strategy": "TRANSFER_PROVIDE",
  "sourceLibraryId": 2,
  "targetLibraryId": 1,
  "copyId": 1001,
  "reserveHours": 24,
  "message": "调拨已完成，已为您预留24小时，请尽快取书"
}
```

#### 场景C：预约排队（全馆无库存）

**条件示例**：理科馆0本，文科馆0本（全部被借出）

**响应示例**：

```json
{
  "taskId": "TASK-1772004850951",
  "status": "SUCCESS",
  "strategy": "RESERVATION",
  "reservationId": 5,
  "pickupLibraryId": 1,
  "reserveHours": 72,
  "message": "全馆暂无库存，已为您预约（3天内有效），书到货后将优先通知您取书"
}
```

#### 失败场景：权限不足

**条件示例**：用户已达最大借阅数，或已借阅该书

**响应示例**：

```json
{"message":"您已借阅该书（可能在调拨中或待取），请勿重复操作","taskId":"TASK-1772115611924","status":"FAILED"}
```

### 数据库变更

**策略A（本地借阅）**：

- **写入**：`tb_book_borrow`（状态`BORROWING`），`tb_book_copy`（状态改为`BORROWED`，`local_borrow_count`+1）
- **更新**：`tb_user`（`current_borrow_count`+1）

**策略B（跨馆调拨）**：

- **写入**：`tb_book_transfer`（调拨记录），`tb_book_borrow`（状态`RESERVED`，24小时过期时间）
- **更新**：`tb_book_copy`（`library_id`改为目标馆，状态`RESERVED`），`tb_library_biblio_stats`（两馆库存同步增减），`tb_user`（`current_borrow_count`+1）

**策略C（预约）**：

- **写入**：`tb_book_reservation`（状态`PENDING`，3天过期时间）

### 测试场景矩阵

| 测试场景         | 初始数据设置                | 预期策略         | 关键验证点                                                   |
| ---------------- | :-------------------------- | :--------------- | :----------------------------------------------------------- |
| 本地借阅情况     | 理科馆有1本AVAILABLE        | LOCAL_LOAN       | `tb_book_copy.status='BORROWED'`，无调拨记录                 |
| 跨馆调拨情况     | 理科馆0本，文科馆2本        | TRANSFER_PROVIDE | `tb_book_transfer`有记录，副本状态`RESERVED`，统计表库存同步 |
| 预约排队情况     | 全馆0本（均借出）           | RESERVATION      | `tb_book_reservation`有PENDING记录，无调拨                   |
| 重复借书限制情况 | 用户已借过该书              | FAILED           | 返回权限错误，无数据库变更                                   |
| 最大借阅限制情况 | 用户current_borrow_count=10 | FAILED           | 返回权限错误                                                 |

## 功能2：**还书自动分配与调拨履约（Smart Return & Auto-Fulfillment）**

### 功能描述

用户还书时，系统自动检查该书目是否有人预约，实现**"还书即分配"**的零等待体验：

- **就地预留（LOCAL_RESERVE）**：预约者期望取书馆与还书馆相同，直接锁定副本24小时
- **跨馆调拨（TRANSFER_FULFILL）**：预约者在其他分馆，触发物流调拨，到馆后锁定24小时
- **预约超期释放（RESERVE_EXPIRE）**：24小时内未取书，自动释放给下一位预约者或转为可借
- **信用分计算（CREDIT_PENALTY）**：逾期归还扣除信用分（每天2分，最高20分），低于60分冻结账号

### 参与智能体

| 智能体                  | 角色                   | 职责                                                 |
| :---------------------- | :--------------------- | :--------------------------------------------------- |
| ReturnAgent             | Manager（协调者）      | 接收还书请求、信用分计算、触发预约队列检查、协调调拨 |
| TransferAgent           | Contractor（物流履约） | 管理调拨状态流转、运输完成回调、库存同步             |
| ReservationMonitorAgent | Monitor（监控者）      | 定时扫描预留超期记录、自动释放、级联分配             |

### 业务流程（时序）

1. **权限校验**：ReturnAgent确认借书记录存在、借书人身份匹配、状态为BORROWING
2. **基础还书**：更新副本为AVAILABLE、关闭借阅记录、减少用户借阅计数、增加还书馆库存
3. **信用评估**：检查是否逾期，扣除信用分（如逾期），冻结账号（如信用分<60）
4. **预约队列遍历**：按FIFO顺序检查`tb_book_reservation`中该`biblio_id`的PENDING记录：
   - 如果预约者已达借阅上限/账号冻结，标记CANCELED，继续下一个
   - 直到找到有效预约者或队列为空
5. **策略执行**：
   - **就地预留**：副本状态改为RESERVED，创建借阅记录（状态RESERVED，24小时过期）
   - **跨馆调拨**：创建调拨记录（状态IN_TRANSIT），副本状态改为IN_TRANSFER，通知物流系统
6. **调拨到货回调**：TransferAgent接收物流系统回调，更新调拨状态为COMPLETED，副本移至目标馆，创建借阅记录（状态RESERVED，24小时过期），通知预约者取书
7. **预留超期处理**：ReservationMonitorAgent每小时扫描，将过期RESERVED记录改为EXPIRED，副本释放，触发下一次预约队列检查

### 核心算法

**预约者优先级评分**（仅用于 Debug 日志，实际为 FIFO）：

```plain
priority = reserve_time（早者优先）
```

**逾期信用分计算**：

```plain
penalty = min(overdue_days × 2, 20)
new_credit = max(0, old_credit - penalty)
if new_credit < 60 then status = FROZEN
```

### 输入参数

#### 接口1：还书（ReturnAgent）

| 字段            | 类型 | 必填 | 说明     | 示例        |
| :-------------- | :--- | :--- | :------- | :---------- |
| copyId          | Long | 是   | 副本ID   | 1           |
| userId          | Long | 是   | 还书人ID | 1           |
| returnLibraryId | Long | 是   | 还书馆ID | 1（理科馆） |

**请求示例**：

```json
{
  "copyId": 1,
  "userId": 1,
  "returnLibraryId": 1
}
```

### 输出结果（按策略区分）

**注意**：`fulfillmentType`字段标识预约兑现方式，`nextAction`提示用户下一步操作。

| 字段              | 类型     | 说明                                    | 适用策略                         |
| :---------------- | :------- | :-------------------------------------- | :------------------------------- |
| taskId            | String   | 任务唯一标识                            | 所有                             |
| status            | String   | SUCCESS / FAILED                        | 所有                             |
| fulfillmentType   | String   | NONE / LOCAL_RESERVE / TRANSFER_FULFILL | 所有                             |
| message           | String   | 详细提示信息                            | 所有                             |
| copyId            | Long     | 归还的副本ID                            | 所有                             |
| reservatorId      | Long     | 预约者ID（如有）                        | LOCAL_RESERVE / TRANSFER_FULFILL |
| reservationId     | Long     | 预约记录ID                              | LOCAL_RESERVE / TRANSFER_FULFILL |
| reserveExpireTime | DateTime | 预留过期时间（24小时后）                | LOCAL_RESERVE / TRANSFER_FULFILL |
| transferId        | Long     | 调拨单ID（跨馆时）                      | TRANSFER_FULFILL                 |
| creditPenalty     | Integer  | 扣除的信用分（0表示无逾期）             | 所有                             |
| nextAction        | String   | WAIT_FOR_PICKUP / TRANSPORTING / NONE   | 所有                             |

### 不同场景的响应示例

#### 场景A：无预约，直接还书

**条件**：该书目无PENDING预约
**响应示例**：

```json
{
  "code": 200,
  "message": "还书成功，书已回到馆1架上",
  "data": {
    "success": true,
    "status": "SUCCESS",
    "message": "还书成功，书已回到馆1架上",
    "hasReservation": false,
    "reservatorId": null,
    "localReservation": false,
    "reservationId": null,
    "reserveExpireTime": null,
    "transferId": null
  }
}
```

#### 场景B：就地预留

**条件**：用户A预约了该书，期望取书馆=还书馆（理科馆）
**响应示例**：

```json
{
  "success": true,
  "status": "SUCCESS",
  "message": "还书成功，书已直接预留给预约用户（24小时内有效）",
  "hasReservation": true,
  "reservatorId": 1,
  "localReservation": true,
  "reservationId": 9,
  "reserveExpireTime": "2026-02-27T19:37:47.7998726",
  "transferId": null
}
```

#### 场景C：跨馆调拨

**条件**：用户B预约了该书，期望取书馆=文科馆（id=2），还书馆=理科馆（id=1）
**响应示例**：

```json
{
  "success": true,
  "status": "SUCCESS",
  "message": "还书成功，已触发向预约用户指定馆的调拨（预计30分钟到达）",
  "hasReservation": true,
  "reservatorId": 1,
  "localReservation": false,
  "reservationId": 10,
  "reserveExpireTime": "2026-02-27T19:38:42.1529019",
  "transferId": 10
}
```

#### 失败场景：非借书人还书

**响应示例**：

```json
{
  "success": false,
  "status": "FAILED",
  "message": "非借书人本人，无法还书（当前借书人ID: 3）",
  "hasReservation": false
}
```

### 数据库变更

**基础还书（所有场景）**：

- **更新**：`tb_book_copy`（状态AVAILABLE，`library_id`改为还书馆，`last_borrow_time`更新）
- **更新**：`tb_book_borrow`（状态RETURNED，`return_time`写入）
- **更新**：`tb_user`（`current_borrow_count`-1，如逾期则`credit_score`-扣分，如信用分<60则`status`改为FROZEN）
- **更新**：`tb_library_biblio_stats`（还书馆`stock_count`+1）

**就地预留（场景B）**：

- **更新**：`tb_book_copy`（状态RESERVED）
- **更新**：`tb_book_reservation`（状态FULFILLED，`copy_id`写入，`fulfill_time`写入，`expire_time`=当前时间+24小时）
- **写入**：`tb_book_borrow`（状态RESERVED，`due_time`=当前时间+24小时）
- **更新**：`tb_user`（预约者`current_borrow_count`+1）

**跨馆调拨（场景C）**：

- **写入**：`tb_book_transfer`（状态IN_TRANSIT，`from_library_id`=还书馆，`to_library_id`=预约者馆）
- **更新**：`tb_book_copy`（状态IN_TRANSFER或保持RESERVED，`library_id`逻辑上属于目标馆但物理未到达）
- **更新**：`tb_book_reservation`（状态FULFILLED，`copy_id`写入，`fulfill_time`写入）
- **更新**：`tb_library_biblio_stats`（还书馆`stock_count`-1，目标馆暂不+1，等到货后+1）

**调拨到货回调（TransferAgent）**：

- **更新**：`tb_book_transfer`（状态COMPLETED，`complete_time`写入）
- **更新**：`tb_book_copy`（`library_id`改目标馆，状态RESERVED）
- **更新**：`tb_library_biblio_stats`（目标馆`stock_count`+1）
- **写入**：`tb_book_borrow`（状态RESERVED，预约者`current_borrow_count`+1，24小时过期）

**预留超期释放（ReservationMonitorAgent）**：

- **更新**：`tb_book_reservation`（状态EXPIRED）
- **更新**：`tb_book_copy`（状态AVAILABLE）
- **触发**：重新调用预约队列检查（同还书逻辑，查找下一个预约者）

### 测试场景矩阵

| 测试场景         | 初始数据设置                            | 预期策略                 | 关键验证点                                                   |
| :--------------- | :-------------------------------------- | :----------------------- | :----------------------------------------------------------- |
| 无预约还书       | tb_book_reservation无记录               | NONE                     | 副本状态AVAILABLE，库存+1，无借阅记录创建                    |
| 就地预留成功     | 馆1有预约，pickup_library_id=1          | LOCAL_RESERVE            | 副本状态RESERVED，预约状态FULFILLED，借阅记录状态RESERVED（过期时间24h后），预约者current_borrow_count+1 |
| 跨馆调拨触发     | 馆1还书，馆2有预约                      | TRANSFER_FULFILL         | tb_book_transfer有记录，副本状态IN_TRANSFER，馆1库存-1，馆2库存不变（到货后+1） |
| 调拨到货回调     | 存在IN_TRANSIT的调拨单                  | TRANSFER_COMPLETE        | 调拨单状态COMPLETED，副本移至馆2，馆2库存+1，创建RESERVED借阅记录，发送通知 |
| 预约者达上限跳过 | 队列：用户A（10/10本），用户B（2/10本） | LOCAL_RESERVE（给用户B） | 用户A预约状态CANCELED，用户B预约状态FULFILLED，副本RESERVED给用户B |
| 24小时预留超期   | 存在已过期（>24h）的RESERVED借阅记录    | RESERVE_EXPIRE           | 借阅记录状态EXPIRED，副本状态AVAILABLE，触发下一次预约检查（如有则分配给下一个） |
| 逾期还书扣分     | 应还时间为3天前                         | CREDIT_PENALTY           | 用户credit_score-6，如低于60则status改为FROZEN               |
| 重复还书         | 同一副本连续还两次                      | FAILED                   | 第二次返回"该书当前无借阅记录"                               |

------

## 功能3：调拨完成回调（Transfer Complete Callback）

### 功能描述

当图书通过物流从源馆运输到目标馆后，由物流人员或馆员确认收货，系统通过此接口完成调拨闭环。根据调拨类型（借书调拨 、还书调拨）采取不同策略：

- **借书调拨**：更新已有的运输中借阅记录为预留状态，启动24小时取书倒计时
- **还书调拨**：为预约用户创建新的预留借阅记录，启动24小时取书倒计时

### 参与智能体

| 智能体        | 角色               | 职责                                                  |
| :------------ | :----------------- | :---------------------------------------------------- |
| TransferAgent | Executor（执行者） | 验证调拨单、更新库存、创建/更新借阅记录、同步用户计数 |

### 业务流程（时序）

1. **接收回调**：TransferController接收调拨完成请求，校验参数合法性
2. **状态校验**：TransferService查询调拨单，确认状态为`IN_TRANSIT`（运输中）
3. **更新调拨单**：将调拨状态改为`COMPLETED`，记录实际到达时间
4. **实物上架**：更新图书副本物理位置到目标馆，状态改为`RESERVED`（已预留），设置默认位置为"馆X-调拨暂存区"
5. **库存同步**：目标馆库存统计+1（物理到货）
6. **策略分支**：
   - **分支A（借书调拨）**：查询`tb_book_borrow`中`TRANSFERRING`状态的记录，更新为`RESERVED`，设置24小时过期时间（用户计数已在发起时增加，此处不再增加）
   - **分支B（还书调拨）**：查询`tb_book_reservation`中`FULFILLED`状态的记录，创建新的`RESERVED`借阅记录，用户借阅计数+1
7. **结果返回**：返回创建的借阅记录ID和预留过期时间

### 核心算法

**调拨类型判定逻辑**：

```java
if (存在TRANSFERRING状态的Borrow记录) {
    //借书调拨：发起时创建了Borrow记录，只需更新状态
    更新Borrow为RESERVED;
} else {
    //还书调拨：发起时创建了Reservation，需新建Borrow并增加用户计数
    创建Borrow(RESERVED);
    用户计数+1;
}
```

### 输入参数

| 字段             | 类型     | 必填 | 说明                           | 示例                |
| :--------------- | :------- | :--- | :----------------------------- | :------------------ |
| transferId       | Long     | 是   | 调拨单ID                       | 20                  |
| actualArriveTime | DateTime | 否   | 实际到达时间（默认为当前时间） | 2026-02-26T22:30:00 |

**请求示例**：

```json
{
  "transferId": 20,
  "actualArriveTime": "2026-02-26T22:30:00"
}
```

### 输出结果（按策略区分）

| 字段              | 类型     | 说明                           | 适用策略 |
| :---------------- | :------- | :----------------------------- | :------- |
| success           | boolean  | 是否成功                       | 所有     |
| message           | String   | 详细提示信息                   | 所有     |
| borrowId          | Long     | 预留借阅记录ID（用户取书凭证） | 成功时   |
| reserveExpireTime | DateTime | 预留过期时间（24小时后）       | 成功时   |

### 不同场景的响应示例

#### 场景A：借书调拨完成（发起时已有Borrow记录）

**条件**：用户借书触发的调拨，运输到达目标馆
**响应示例**：

```json
{
  "code": 200,
  "message": "调拨完成，图书已预留给用户1",
  "data": {
    "success": true,
    "message": "调拨完成，图书已预留给用户1",
    "borrowId": 10,
    "reserveExpireTime": "2026-02-28T20:06:27.5181156"
  }
}
```

#### 场景B：还书调拨完成（发起时无Borrow记录）

**条件**：用户还书触发的调拨，满足其他馆预约者
**响应示例**：

```json
{
  "success": true,
  "message": "调拨完成，图书已预留给用户8",
  "borrowId": 101,
  "reserveExpireTime": "2026-02-27T22:30:00"
}
```

#### 失败场景：调拨单不存在

**响应示例**：

```json
{
  "success": false,
  "message": "调拨单不存在",
  "borrowId": null,
  "reserveExpireTime": null
}
```

#### 失败场景：状态异常（非运输中）

**响应示例**：

```json
{
  "success": false,
  "message": "调拨单状态异常，当前状态：COMPLETED",
  "borrowId": null,
  "reserveExpireTime": null
}
```

### 数据库变更

**所有场景**：

- **更新**：`tb_book_transfer`（status→COMPLETED，complete_time写入）
- **更新**：`tb_book_copy`（library_id→目标馆，status→RESERVED，location→"馆X-调拨暂存区"）
- **更新**：`tb_library_biblio_stats`（目标馆stock_count+1）

**借书调拨场景（分支A）**：

- **更新**：`tb_book_borrow`（status→RESERVED，due_time→当前时间+24小时）

**还书调拨场景（分支B）**：

- **写入**：`tb_book_borrow`（status→RESERVED，due_time→当前时间+24小时）
- **更新**：`tb_user`（current_borrow_count+1）

### 测试场景矩阵

| 测试场景     | 前置条件                                                 | 预期策略   | 关键验证点                                                   |
| :----------- | :------------------------------------------------------- | :--------- | :----------------------------------------------------------- |
| 借书调拨完成 | 存在IN_TRANSIT调拨单，发起时创建了TRANSFERRING的Borrow   | 更新Borrow | 不新增Borrow记录，不增加用户计数，状态变为RESERVED，副本移至目标馆 |
| 还书调拨完成 | 存在IN_TRANSIT调拨单，发起时创建了FULFILLED的Reservation | 新建Borrow | 新增Borrow记录，用户计数+1，状态变为RESERVED，副本移至目标馆 |
| 重复完成     | 调拨单状态已为COMPLETED                                  | FAILED     | 返回"状态异常"，无数据库变更                                 |
| 调拨单不存在 | 传入不存在的transferId                                   | FAILED     | 返回"调拨单不存在"                                           |

---

## 功能4：**取书确认（Pickup Confirmation）**

### 功能描述

用户到馆后确认取书,完成借阅流程。系统验证用户身份、预留状态和预留时间,将借阅状态从`RESERVED`(已预留)转换为`BORROWING`(借阅中),正式开始借阅计时。

### 业务规则

- **仅限本人取书**: 必须是预约人本人,不支持代取书
- **24小时有效期**: 必须在预留期内取书,超期需重新预约
- **状态校验**: 只有`RESERVED`状态的记录才能取书
- **借阅期限**: 取书确认后开始计算借阅期限(默认30天)

### 执行流程

1. **接收请求**: PickupController接收取书确认请求
2. **身份验证**: 验证用户是否为预约人本人
3. **状态校验**: 检查借阅状态是否为`RESERVED`
4. **时间校验**: 检查是否在24小时预留期内
5. **状态更新**: 
   - 借阅记录: `RESERVED` → `BORROWING`
   - 图书副本: `RESERVED` → `BORROWED`
6. **时间记录**: 记录实际取书时间,计算应还日期
7. **结果返回**: 返回取书成功信息和借阅详情

### 输入参数

| 字段     | 类型 | 必填 | 说明         | 示例 |
| :------- | :--- | :--- | :----------- | :--- |
| borrowId | Long | 是   | 借阅记录ID   | 100  |
| userId   | Long | 是   | 用户ID       | 5    |

**请求示例**：

```json
{
  "borrowId": 100,
  "userId": 5
}
```

### 输出结果

| 字段       | 类型     | 说明           |
| :--------- | :------- | :------------- |
| code       | Integer  | 状态码(200成功,400失败) |
| message    | String   | 提示信息       |
| data       | Object   | 响应数据       |
| └─ borrowId    | Long     | 借阅记录ID     |
| └─ bookTitle   | String   | 图书名称       |
| └─ borrowDate  | DateTime | 借阅日期       |
| └─ dueDate     | DateTime | 应还日期       |
| └─ libraryName | String   | 取书馆名称     |

### 不同场景的响应示例

#### 场景A：取书成功

**条件**：用户身份正确,预留状态正常,在有效期内
**响应示例**：

```json
{
  "code": 200,
  "message": "取书成功",
  "data": {
    "borrowId": 100,
    "bookTitle": "Java编程思想",
    "borrowDate": "2026-02-27T19:14:33",
    "dueDate": "2026-03-29T19:14:33",
    "libraryName": "理科馆"
  }
}
```

#### 场景B：借阅记录不存在

**条件**：传入的borrowId不存在
**响应示例**：

```json
{
  "code": 400,
  "message": "借阅记录不存在",
  "data": null
}
```

#### 场景C：非本人取书

**条件**：userId与借阅记录的userId不匹配
**响应示例**：

```json
{
  "code": 400,
  "message": "仅限预约人本人取书",
  "data": null
}
```

#### 场景D：预留已超期

**条件**：当前时间超过pickupDeadline
**响应示例**：

```json
{
  "code": 400,
  "message": "预留已超期,请重新预约",
  "data": null
}
```

#### 场景E：状态异常

**条件**：借阅状态不是RESERVED
**响应示例**：

```json
{
  "code": 400,
  "message": "借阅状态异常,当前状态: BORROWING",
  "data": null
}
```

### 数据库变更

**所有成功场景**：

- **更新**：`tb_book_borrow`
  - status: `RESERVED` → `BORROWING`
  - actual_pickup_time: 写入当前时间
  - borrow_time: 写入当前时间
  - due_time: 写入当前时间+30天

- **更新**：`tb_book_copy`
  - status: `RESERVED` → `BORROWED`
  - last_borrow_time: 写入当前时间
  - local_borrow_count: +1

### 测试场景矩阵

| 测试场景     | 前置条件                           | 预期结果 | 关键验证点                           |
| :----------- | :--------------------------------- | :------- | :----------------------------------- |
| 正常取书     | RESERVED状态,在有效期内,本人取书   | 成功     | 状态变为BORROWING,记录取书时间       |
| 非本人取书   | userId与借阅记录不匹配             | 失败     | 返回"仅限预约人本人取书"             |
| 预留超期     | 当前时间超过pickupDeadline         | 失败     | 返回"预留已超期,请重新预约"          |
| 状态异常     | 状态不是RESERVED                   | 失败     | 返回"借阅状态异常"                   |
| 记录不存在   | borrowId不存在                     | 失败     | 返回"借阅记录不存在"                 |

---

## 功能5：**主动调拨与库存平衡（Proactive Transfer & Inventory Balance）**

### 功能描述

系统基于馆藏书目统计表（`tb_library_biblio_stats`）的借阅热度分析，自动触发低利用率馆向高需求馆的主动调拨，实现库存动态平衡，提高资源利用率。

系统支持两种审批模式：
- **自动审批**：调拨数量≤3本时，系统自动审批并执行
- **人工审批**：调拨数量>3本时，生成调拨建议等待馆员审批

### 参与智能体

| 智能体 | 角色 | 职责 |
| :----- | :--- | :--- |
| InventoryBalanceAgent | 库存平衡代理 | 定期分析库存、生成调拨建议、执行调拨 |

### 执行流程

1. **库存分析**：
   - 查询所有图书馆的书目统计信息
   - 识别需求旺盛的馆（目标馆）：库存<2本 且 （近30天借出>20次 或 预约排队>3人）
   - 识别库存充足的馆（源馆）：库存>5本 且 近30天借出<10次

2. **生成调拨建议**：
   - 为每个需求馆寻找最佳供应馆
   - 计算调拨数量：min(源馆库存×30%, 目标馆需求缺口, 5本)
   - 计算优先级评分：目标馆需求度×0.4 + 源馆富余度×0.3 + 距离成本×0.2 + 历史调拨频率×0.1
   - 生成调拨建议并保存到`tb_transfer_suggestion`表

3. **审批执行**：
   - 小批量调拨（≤3本）：自动审批并执行
   - 大批量调拨（>3本）：通知馆员审批（TODO: 管理员系统未实现，暂时控制台打印）
   - 审批通过后创建调拨单

4. **调拨执行**：
   - 查找源馆的可用副本
   - 创建调拨单（状态：IN_TRANSIT）
   - 更新副本状态（IN_TRANSIT）
   - 更新调拨建议状态（EXECUTED）

### 核心算法

**调拨数量计算公式**：

```plain
调拨数量 = min(
    源馆可用库存 × 0.3,  // 最多调出源馆30%的库存
    目标馆需求缺口,       // 目标馆需要的数量
    5                     // 单次最多调拨5本
)
```

**优先级评分公式**：

```plain
优先级评分 = 
    目标馆需求度 × 0.4 +    // 近30天借出次数、预约排队人数
    源馆富余度 × 0.3 +      // 库存数量、利用率
    距离成本 × 0.2 +        // 馆与馆之间的距离
    历史调拨频率 × 0.1      // 历史调拨成功率
```

### 输入参数

**手动触发接口**：

| 字段 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| 无 | - | - | 手动触发无需参数 | - |

**审批接口**：

| 字段 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| id | Long | 是 | 调拨建议ID | 1 |
| approverId | Long | 是 | 审批人ID | 1 |
| reason | String | 否 | 拒绝原因（拒绝时使用） | "测试拒绝" |

### 输出结果

**调拨建议列表**：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | Long | 调拨建议ID |
| fromLibraryId | Long | 源馆ID |
| toLibraryId | Long | 目标馆ID |
| biblioId | Long | 书目ID |
| suggestedQuantity | Integer | 建议调拨数量 |
| priorityScore | BigDecimal | 优先级评分 |
| reason | String | 调拨原因 |
| status | String | 状态:PENDING/APPROVED/REJECTED/EXECUTED |
| approverId | Long | 审批人ID |
| approveTime | DateTime | 审批时间 |
| createTime | DateTime | 创建时间 |

### 不同场景的响应示例

#### 场景A：手动触发库存平衡分析

**请求**：

```
POST /api/transfer/trigger-balance
```

**响应示例**：

```json
{
  "code": 200,
  "message": "库存平衡分析已触发",
  "data": null
}
```

#### 场景B：查询待审批的调拨建议

**请求**：

```
GET /api/transfer/suggestion/list
```

**响应示例**：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "fromLibraryId": 2,
      "toLibraryId": 1,
      "biblioId": 1,
      "suggestedQuantity": 3,
      "priorityScore": 85.50,
      "reason": "理科馆需求旺盛（库存1，近30天借出25次），文科馆库存充足（库存8，近30天借出5次）",
      "status": "PENDING",
      "approverId": null,
      "approveTime": null,
      "createTime": "2026-03-07T22:00:00"
    }
  ]
}
```

#### 场景C：审批通过调拨建议

**请求**：

```
POST /api/transfer/suggestion/approve/1?approverId=1
```

**响应示例**：

```json
{
  "code": 200,
  "message": "审批通过",
  "data": null
}
```

#### 场景D：拒绝调拨建议

**请求**：

```
POST /api/transfer/suggestion/reject/1?approverId=1&reason=测试拒绝
```

**响应示例**：

```json
{
  "code": 200,
  "message": "已拒绝",
  "data": null
}
```

### 数据库变更

**新增表**：`tb_transfer_suggestion`（调拨建议表）

**调拨表补充字段**：`tb_book_transfer`
- `estimated_arrival_time`：预计到达时间
- `actual_arrival_time`：实际到达时间
- `transfer_reason`：调拨原因（USER_REQUEST/INVENTORY_BALANCE）
- `suggestion_id`：关联的调拨建议ID

### 定时任务

- **执行时间**：每天凌晨2点
- **Cron表达式**：`0 0 2 * * ?`
- **任务类**：`InventoryBalanceTask`

### API接口列表

| 接口 | 方法 | 说明 |
| :--- | :--- | :--- |
| /api/transfer/suggestion/list | GET | 查询调拨建议列表 |
| /api/transfer/suggestion/{id} | GET | 查询调拨建议详情 |
| /api/transfer/suggestion/approve/{id} | POST | 审批通过调拨建议 |
| /api/transfer/suggestion/reject/{id} | POST | 拒绝调拨建议 |
| /api/transfer/trigger-balance | POST | 手动触发库存平衡分析 |
| /api/transfer/complete-batch | POST | 批量调拨完成回调 |

### 批量调拨完成接口

**接口路径**：`POST /api/transfer/complete-batch`

**功能描述**：物流系统或馆员确认批量调拨图书已到达目标馆，完成批量调拨闭环

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| orderId | Long | 是 | 调拨单ID |
| actualArriveTime | DateTime | 否 | 实际到达时间（可选，默认为当前时间） |

**请求示例**：
```json
{
  "orderId": 1,
  "actualArriveTime": "2026-03-08T20:00:00"
}
```

**响应结果**：

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| code | Integer | 状态码(200成功,400失败) |
| message | String | 提示信息 |
| data | Object | 响应数据 |
| └─ orderId | Long | 调拨单ID |
| └─ completedCount | Integer | 成功调拨数量 |

**响应示例**：
```json
{
  "code": 200,
  "message": "批量调拨完成，成功调拨5本",
  "data": {
    "orderId": 1,
    "completedCount": 5
  }
}
```

**处理流程**：
1. 校验调拨单存在性和状态(必须为IN_PROGRESS或COMPLETED)
2. 查询所有运输中的调拨记录
3. 批量更新调拨记录状态为COMPLETED
4. 批量更新副本状态为AVAILABLE(直接上架)
5. 更新目标馆库存(+N)
6. 更新调拨单状态为COMPLETED
7. 返回结果

**关键特点**：
- **跳过用户逻辑**：INVENTORY_BALANCE场景没有用户,直接上架
- **批量操作**：一次处理多本图书
- **事务保证**：使用@Transactional确保数据一致性
- **纯三层架构**：不涉及Agent,纯业务逻辑处理

### 测试场景矩阵

| 测试场景 | 前置条件 | 预期结果 | 关键验证点 |
| :--------- | :--------- | :--------- | :----------- |
| 自动审批执行 | 调拨数量≤3本 | 自动创建调拨单 | 建议状态EXECUTED，调拨单创建成功 |
| 人工审批执行 | 调拨数量>3本 | 等待审批 | 建议状态PENDING，控制台打印通知 |
| 审批通过 | 建议状态PENDING | 创建调拨单 | 建议状态APPROVED→EXECUTED |
| 审批拒绝 | 建议状态PENDING | 不创建调拨单 | 建议状态REJECTED |
| 无需调拨 | 所有馆库存均衡 | 不生成建议 | 建议列表为空 |
| 手动触发 | 任意状态 | 立即执行分析 | 控制台打印分析日志 |

### 已知问题和TODO

1. **调拨执行已完善**：
   - ✅ 已实现调拨单创建和调拨记录创建
   - ✅ 已实现副本状态更新和库存减少
   - ✅ 已实现批量调拨完成接口

2. **通知功能未实现**：
   - 大批量调拨时，控制台打印通知
   - TODO: 实现管理员系统后，集成通知功能

3. **距离成本简化**：
   - 当前距离成本固定为1.0
   - TODO: 实现基于地理位置的距离计算

4. **历史调拨频率未实现**：
   - 当前历史调拨频率固定为0
   - TODO: 实现历史调拨频率统计

### 功能完善总结

**批量调拨功能已完整实现**：

1. **调拨建议生成**：
   - 定时任务分析库存分布
   - 生成调拨建议并计算优先级
   - 支持自动审批和人工审批

2. **调拨执行**：
   - 创建调拨单管理批量调拨
   - 创建多条调拨记录关联同一调拨单
   - 更新副本状态为IN_TRANSIT
   - 减少源馆库存

3. **调拨完成**：
   - 新增批量调拨完成接口
   - 批量更新调拨记录状态
   - 批量更新副本状态为AVAILABLE(直接上架)
   - 增加目标馆库存
   - 更新调拨单状态

**数据流程**：
```
调拨建议(建议5本)
  → 审批通过
  → 创建调拨单(记录批量调拨信息)
  → 创建5条调拨记录(共享order_id)
  → 执行调拨(副本状态IN_TRANSIT,源馆库存-5)
  → 调拨完成(副本状态AVAILABLE,目标馆库存+5)
```

**技术特点**：
- 标准三层架构设计
- 事务管理保证数据一致性
- 详细的日志记录
- 完整的异常处理

---

## 功能6：**调拨进度跟踪与通知服务（Transfer Progress & Notification）**

### 功能描述

为调拨功能提供进度查询和通知服务,提升用户体验：

- **调拨进度跟踪**：查询单个调拨、批量调拨的实时进度,包括进度百分比、时间信息等
- **调拨通知服务**：发送调拨发起、到达通知(仅用户请求调拨)
- **延迟检测告警**：定时检测超时调拨并控制台打印告警

系统采用简化设计,不使用Redis缓存和事件驱动,直接同步调用,简单高效。

### 参与组件

| 组件 | 角色 | 职责 |
| :--- | :--- | :--- |
| TransferProgressController | Controller | 提供进度查询接口 |
| TransferProgressService | Service | 进度计算和数据组装 |
| TransferNotificationService | Service | 发送调拨通知 |
| TransferDelayCheckTask | Task | 定时检测延迟调拨 |

### 核心流程

#### 1. 调拨进度查询流程

1. **接收请求**：用户或管理员调用进度查询接口
2. **查询数据**：从数据库查询调拨记录、调拨单、图书、馆信息
3. **计算进度**：根据状态和时间计算进度百分比
   - PENDING: 0%
   - IN_TRANSIT: (已用时间/预计时间) × 100,最大95%
   - COMPLETED: 100%
   - CANCELED: 0%
4. **组装数据**：构建响应DTO,包含图书信息、馆信息、时间信息等
5. **返回结果**：返回进度详情

#### 2. 调拨通知流程

1. **触发通知**：调拨创建或完成时调用通知服务
2. **判断类型**：检查调拨原因(USER_REQUEST/INVENTORY_BALANCE)
3. **发送通知**：
   - USER_REQUEST: 发送站内信给目标用户
   - INVENTORY_BALANCE: 不发送通知(系统内部操作)
4. **记录通知**：写入`tb_notification`表

#### 3. 延迟检测流程

1. **定时触发**：每小时执行一次(Cron: `0 0 * * * ?`)
2. **查询延迟**：查询状态为IN_TRANSIT且超过预计到达时间的调拨记录
3. **打印告警**：控制台输出详细告警信息(调拨ID、图书、源馆、目标馆、延迟时长)

### 核心算法

**进度百分比计算公式**：

```plain
progress = (usedTime / estimatedTime) × 100
最大值: 95% (到达后才会变为100%)
```

**时长格式化规则**：
- 小于1小时: "X分钟"
- 1-24小时: "X小时Y分钟"
- 大于24小时: "X天Y小时"

### 输入参数

#### 接口1：查询单个调拨进度

| 字段 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| transferId | Long | 是 | 调拨记录ID(路径参数) | 1 |

**请求示例**：

```
GET /api/transfer/progress/1
```

#### 接口2：查询批量调拨进度

| 字段 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| orderId | Long | 是 | 调拨单ID(路径参数) | 1 |

**请求示例**：

```
GET /api/transfer/progress/batch/1
```

#### 接口3：查询所有调拨记录

| 字段 | 类型 | 必填 | 说明 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| status | String | 否 | 状态筛选 | IN_TRANSIT |
| page | int | 否 | 页码(默认1) | 1 |
| size | int | 否 | 每页数量(默认10) | 10 |

**请求示例**：

```
GET /api/transfer/list?status=IN_TRANSIT&page=1&size=10
```

### 输出结果

#### 单个调拨进度响应

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| transferId | Long | 调拨记录ID |
| copyId | Long | 副本ID |
| bookInfo | Object | 图书信息(title, author, isbn) |
| fromLibrary | Object | 源馆信息(id, name, location) |
| toLibrary | Object | 目标馆信息(id, name, location) |
| status | String | 状态(PENDING/IN_TRANSIT/COMPLETED/CANCELED) |
| statusText | String | 状态文本(待处理/运输中/已完成/已取消) |
| progressPercentage | Integer | 进度百分比(0-100) |
| timeInfo | Object | 时间信息(requestTime, estimatedArrivalTime, actualArrivalTime, usedDuration, remainingDuration) |
| transferReason | String | 调拨原因(USER_REQUEST/INVENTORY_BALANCE) |
| transferReasonText | String | 调拨原因文本(用户请求/库存平衡) |

#### 批量调拨进度响应

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| orderId | Long | 调拨单ID |
| biblioId | Long | 书目ID |
| bookInfo | Object | 图书信息 |
| fromLibrary | Object | 源馆信息 |
| toLibrary | Object | 目标馆信息 |
| status | String | 状态(PENDING/IN_PROGRESS/COMPLETED/CANCELED) |
| statusText | String | 状态文本 |
| progressInfo | Object | 进度信息(totalQuantity, completedQuantity, inTransitQuantity, pendingQuantity, progressPercentage) |
| timeInfo | Object | 时间信息(createTime, completeTime, usedDuration) |
| transferRecords | Array | 调拨记录列表(transferId, copyId, status, statusText, progressPercentage) |

### 不同场景的响应示例

#### 场景A：查询单个调拨进度(运输中)

**条件**：调拨记录存在,状态为IN_TRANSIT

**响应示例**：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "transferId": 1,
    "copyId": 1001,
    "bookInfo": {
      "title": "Java编程思想",
      "author": "Bruce Eckel",
      "isbn": "978-7-111-1"
    },
    "fromLibrary": {
      "id": 2,
      "name": "文科馆",
      "location": "校园东区人文楼"
    },
    "toLibrary": {
      "id": 1,
      "name": "理科馆",
      "location": "校园西区理科楼"
    },
    "status": "IN_TRANSIT",
    "statusText": "运输中",
    "progressPercentage": 60,
    "timeInfo": {
      "requestTime": "2026-03-08T10:00:00",
      "estimatedArrivalTime": "2026-03-08T16:00:00",
      "actualArrivalTime": null,
      "usedDuration": "3小时20分钟",
      "remainingDuration": "预计2小时40分钟"
    },
    "transferReason": "USER_REQUEST",
    "transferReasonText": "用户请求"
  }
}
```

#### 场景B：查询批量调拨进度(执行中)

**条件**：调拨单存在,部分调拨已完成

**响应示例**：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "orderId": 1,
    "biblioId": 1,
    "bookInfo": {
      "title": "Java编程思想",
      "author": "Bruce Eckel"
    },
    "fromLibrary": {
      "id": 2,
      "name": "文科馆"
    },
    "toLibrary": {
      "id": 1,
      "name": "理科馆"
    },
    "status": "IN_PROGRESS",
    "statusText": "执行中",
    "progressInfo": {
      "totalQuantity": 5,
      "completedQuantity": 2,
      "inTransitQuantity": 3,
      "pendingQuantity": 0,
      "progressPercentage": 40
    },
    "timeInfo": {
      "createTime": "2026-03-08T10:00:00",
      "completeTime": null,
      "usedDuration": "3小时20分钟"
    },
    "transferRecords": [
      {
        "transferId": 1,
        "copyId": 1001,
        "status": "COMPLETED",
        "statusText": "已完成",
        "progressPercentage": 100
      },
      {
        "transferId": 2,
        "copyId": 1002,
        "status": "IN_TRANSIT",
        "statusText": "运输中",
        "progressPercentage": 60
      }
    ]
  }
}
```

#### 场景C：调拨记录不存在

**条件**：传入的transferId不存在

**响应示例**：

```json
{
  "code": 400,
  "message": "调拨记录不存在",
  "data": null
}
```

### 通知服务说明

#### 通知场景

| 场景 | 触发时机 | 通知对象 | 通知内容 |
| :--- | :--- | :--- | :--- |
| 调拨发起 | 调拨记录创建时 | 目标用户(USER_REQUEST) | 图书已发出,预计到达时间 |
| 调拨到达 | 调拨完成时 | 目标用户(USER_REQUEST) | 图书已到达,请及时处理 |
| 调拨延迟 | 超过预计时间 | 控制台告警 | 调拨超时,请及时跟进 |

#### 通知规则

- **用户请求调拨(USER_REQUEST)**：发送站内信给目标用户
- **库存平衡调拨(INVENTORY_BALANCE)**：不发送通知(系统内部操作)

#### 延迟检测定时任务

**任务配置**：
- 任务类：`TransferDelayCheckTask`
- 执行频率：每小时一次
- Cron表达式：`0 0 * * * ?`

**告警输出示例**：

```
=== 调拨延迟告警 ===
调拨ID: 1
副本ID: 1001
图书: Java编程思想
源馆: 文科馆
目标馆: 理科馆
预计到达时间: 2026-03-08 16:00
已超时: 2 小时
请及时跟进处理!
===================
```

### 技术实现

**架构设计**：
- 三层架构(简化版)
- 不使用Redis缓存
- 不使用事件驱动
- 直接同步调用

**新增组件**：
- `TransferProgressDTO`：单个调拨进度响应DTO
- `BatchTransferProgressDTO`：批量调拨进度响应DTO
- `TransferProgressService`：进度计算服务
- `TransferProgressController`：进度查询接口
- `TransferNotificationService`：调拨通知服务
- `TransferDelayCheckTask`：延迟检测定时任务

**简化说明**：
- 进度实时计算,不缓存
- 通知同步写入数据库
- 通知模板硬编码
- 延迟告警仅控制台打印

---
