## 功能1：**智能借书与资源调度（Smart Borrowing）**

### 功能描述

用户发起借书请求后，系统通过多智能体协作自动选择最优策略：

- **本地借阅（LOCAL_LOAN）**：目标馆有库存时立即借出
- **跨馆调拨（TRANSFER_PROVIDE）**：目标馆无库存但其他馆有，自动协商调拨并预留24小时
- **预约排队（RESERVATION）**：全馆无库存时创建预约，书归还后自动优先分配

系统内置权限检查（最大借阅数、重复借书限制），确保业务合规。

### 参与智能体

| 智能体              | 角色                 | 职责                                             |
| :------------------ | :------------------- | :----------------------------------------------- |
| UserDemandAgent     | Manager（协调者）    | 接收请求、权限检查、策略选择、调拨执行、预约管理 |
| InventoryAgent-LibN | Contractor（参与者） | 评估本地库存、计算投标评分（距离+压力）、响应CFP |

### 业务流程（时序）

1. **权限检查**：UserDemandAgent检查用户状态、最大借阅数、是否已借该书
2. **策略选择**：
   - **本地优先**：检查`preferredLibraryId`是否有`AVAILABLE`副本 → 直接借书（策略A）
   - **跨馆协商**：本地无库存 → 广播CFP → 收集投标 → 选择最优馆调拨（策略B）
   - **预约兜底**：全馆无库存 → 创建`tb_book_reservation`记录（策略C）
3. **执行与预留**：
   - 策略A：创建借阅记录（`BORROWING`），副本状态改为`BORROWED`
   - 策略B：执行调拨（`tb_book_transfer`），创建借阅记录（`TRANSFERING`），等待书籍调拨完成
   - 策略C：创建预约记录（`PENDING`，3天有效）
4. **结果返回**：返回对应策略的执行结果

### 核心算法

**投标评分公式**（仅策略B使用）：

```plain
score = 80 - (distance(km) × 10) - (pending_reservation_count × 5)
```

### 输入参数

| 字段               | 类型 | 必填 | 说明         | 示例        |
| :----------------- | :--- | :--- | :----------- | :---------- |
| userId             | Long | 是   | 用户ID       | 1           |
| biblioId           | Long | 是   | 书目ID       | 1           |
| preferredLibraryId | Long | 是   | 期望取书馆ID | 1（理科馆） |

**请求示例**：

JSON

```json
{
  "userId": 1,
  "biblioId": 1,
  "preferredLibraryId": 1
}
```

### 输出结果（按策略区分）

**注意**：`strategy`字段标识实际执行的策略，不同策略返回的字段集合不同。

| 字段            | 类型    | 说明                                        | 适用策略                       |
| :-------------- | :------ | :------------------------------------------ | :----------------------------- |
| taskId          | String  | 任务唯一标识                                | 所有                           |
| status          | String  | SUCCESS / FAILED                            | 所有                           |
| strategy        | String  | LOCAL_LOAN / TRANSFER_PROVIDE / RESERVATION | 所有                           |
| message         | String  | 详细提示信息                                | 所有                           |
| copyId          | Long    | 分配的副本ID                                | LOCAL_LOAN / TRANSFER_PROVIDE  |
| libraryId       | Long    | 取书馆ID（本地借阅时）                      | LOCAL_LOAN                     |
| sourceLibraryId | Long    | 来源馆ID（调拨时）                          | TRANSFER_PROVIDE               |
| targetLibraryId | Long    | 目标馆ID（调拨时）                          | TRANSFER_PROVIDE               |
| reserveHours    | Integer | 预留时长（小时）                            | TRANSFER_PROVIDE / RESERVATION |
| reservationId   | Long    | 预约记录ID                                  | RESERVATION                    |
| pickupLibraryId | Long    | 预约取书馆ID                                | RESERVATION                    |

### 不同场景的响应示例

#### 场景A：本地直接借阅（目标馆有库存）

**条件示例**：`preferredLibraryId=1`（理科馆），且理科馆有`AVAILABLE`副本

**响应示例**：

```json
{
  "taskId": "TASK-1772004850949",
  "status": "SUCCESS",
  "strategy": "LOCAL_LOAN",
  "copyId": 1002,
  "libraryId": 1,
  "message": "借书成功，请在30天内归还"
}
```

#### 场景B：跨馆调拨+预留（目标馆无，其他馆有）

**条件示例**：理科馆0本，文科馆2本

**响应示例**：

```json
{
  "taskId": "TASK-1772004850950",
  "status": "SUCCESS",
  "strategy": "TRANSFER_PROVIDE",
  "sourceLibraryId": 2,
  "targetLibraryId": 1,
  "copyId": 1001,
  "reserveHours": 24,
  "message": "调拨已完成，已为您预留24小时，请尽快取书"
}
```

#### 场景C：预约排队（全馆无库存）

**条件示例**：理科馆0本，文科馆0本（全部被借出）

**响应示例**：

```json
{
  "taskId": "TASK-1772004850951",
  "status": "SUCCESS",
  "strategy": "RESERVATION",
  "reservationId": 5,
  "pickupLibraryId": 1,
  "reserveHours": 72,
  "message": "全馆暂无库存，已为您预约（3天内有效），书到货后将优先通知您取书"
}
```

#### 失败场景：权限不足

**条件示例**：用户已达最大借阅数，或已借阅该书

**响应示例**：

```json
{"message":"您已借阅该书（可能在调拨中或待取），请勿重复操作","taskId":"TASK-1772115611924","status":"FAILED"}
```

### 数据库变更

**策略A（本地借阅）**：

- **写入**：`tb_book_borrow`（状态`BORROWING`），`tb_book_copy`（状态改为`BORROWED`，`local_borrow_count`+1）
- **更新**：`tb_user`（`current_borrow_count`+1）

**策略B（跨馆调拨）**：

- **写入**：`tb_book_transfer`（调拨记录），`tb_book_borrow`（状态`RESERVED`，24小时过期时间）
- **更新**：`tb_book_copy`（`library_id`改为目标馆，状态`RESERVED`），`tb_library_biblio_stats`（两馆库存同步增减），`tb_user`（`current_borrow_count`+1）

**策略C（预约）**：

- **写入**：`tb_book_reservation`（状态`PENDING`，3天过期时间）

### 测试场景矩阵

| 测试场景         | 初始数据设置                | 预期策略         | 关键验证点                                                   |
| ---------------- | :-------------------------- | :--------------- | :----------------------------------------------------------- |
| 本地借阅情况     | 理科馆有1本AVAILABLE        | LOCAL_LOAN       | `tb_book_copy.status='BORROWED'`，无调拨记录                 |
| 跨馆调拨情况     | 理科馆0本，文科馆2本        | TRANSFER_PROVIDE | `tb_book_transfer`有记录，副本状态`RESERVED`，统计表库存同步 |
| 预约排队情况     | 全馆0本（均借出）           | RESERVATION      | `tb_book_reservation`有PENDING记录，无调拨                   |
| 重复借书限制情况 | 用户已借过该书              | FAILED           | 返回权限错误，无数据库变更                                   |
| 最大借阅限制情况 | 用户current_borrow_count=10 | FAILED           | 返回权限错误                                                 |

## 功能2：**还书自动分配与调拨履约（Smart Return & Auto-Fulfillment）**

### 功能描述

用户还书时，系统自动检查该书目是否有人预约，实现**"还书即分配"**的零等待体验：

- **就地预留（LOCAL_RESERVE）**：预约者期望取书馆与还书馆相同，直接锁定副本24小时
- **跨馆调拨（TRANSFER_FULFILL）**：预约者在其他分馆，触发物流调拨，到馆后锁定24小时
- **预约超期释放（RESERVE_EXPIRE）**：24小时内未取书，自动释放给下一位预约者或转为可借
- **信用分计算（CREDIT_PENALTY）**：逾期归还扣除信用分（每天2分，最高20分），低于60分冻结账号

### 参与智能体

| 智能体                  | 角色                   | 职责                                                 |
| :---------------------- | :--------------------- | :--------------------------------------------------- |
| ReturnAgent             | Manager（协调者）      | 接收还书请求、信用分计算、触发预约队列检查、协调调拨 |
| TransferAgent           | Contractor（物流履约） | 管理调拨状态流转、运输完成回调、库存同步             |
| ReservationMonitorAgent | Monitor（监控者）      | 定时扫描预留超期记录、自动释放、级联分配             |

### 业务流程（时序）

1. **权限校验**：ReturnAgent确认借书记录存在、借书人身份匹配、状态为BORROWING
2. **基础还书**：更新副本为AVAILABLE、关闭借阅记录、减少用户借阅计数、增加还书馆库存
3. **信用评估**：检查是否逾期，扣除信用分（如逾期），冻结账号（如信用分<60）
4. **预约队列遍历**：按FIFO顺序检查`tb_book_reservation`中该`biblio_id`的PENDING记录：
   - 如果预约者已达借阅上限/账号冻结，标记CANCELED，继续下一个
   - 直到找到有效预约者或队列为空
5. **策略执行**：
   - **就地预留**：副本状态改为RESERVED，创建借阅记录（状态RESERVED，24小时过期）
   - **跨馆调拨**：创建调拨记录（状态IN_TRANSIT），副本状态改为IN_TRANSFER，通知物流系统
6. **调拨到货回调**：TransferAgent接收物流系统回调，更新调拨状态为COMPLETED，副本移至目标馆，创建借阅记录（状态RESERVED，24小时过期），通知预约者取书
7. **预留超期处理**：ReservationMonitorAgent每小时扫描，将过期RESERVED记录改为EXPIRED，副本释放，触发下一次预约队列检查

### 核心算法

**预约者优先级评分**（仅用于 Debug 日志，实际为 FIFO）：

```plain
priority = reserve_time（早者优先）
```

**逾期信用分计算**：

```plain
penalty = min(overdue_days × 2, 20)
new_credit = max(0, old_credit - penalty)
if new_credit < 60 then status = FROZEN
```

### 输入参数

#### 接口1：还书（ReturnAgent）

| 字段            | 类型 | 必填 | 说明     | 示例        |
| :-------------- | :--- | :--- | :------- | :---------- |
| copyId          | Long | 是   | 副本ID   | 1001        |
| userId          | Long | 是   | 还书人ID | 1           |
| returnLibraryId | Long | 是   | 还书馆ID | 1（理科馆） |

**请求示例**：

```json
{
  "copyId": 1001,
  "userId": 1,
  "returnLibraryId": 1
}
```

#### 接口2：调拨完成回调（TransferAgent）（待完成）

| 字段             | 类型     | 必填 | 说明                         | 示例                |
| :--------------- | :------- | :--- | :--------------------------- | :------------------ |
| transferId       | Long     | 是   | 调拨记录ID                   | 50                  |
| actualArriveTime | DateTime | 否   | 实际到达时间（默认当前时间） | 2024-01-15T14:30:00 |

**请求示例**：

```json
{
  "transferId": 50,
  "actualArriveTime": "2024-01-15T14:30:00"
}
```

### 输出结果（按策略区分）

**注意**：`fulfillmentType`字段标识预约兑现方式，`nextAction`提示用户下一步操作。

| 字段              | 类型     | 说明                                    | 适用策略                         |
| :---------------- | :------- | :-------------------------------------- | :------------------------------- |
| taskId            | String   | 任务唯一标识                            | 所有                             |
| status            | String   | SUCCESS / FAILED                        | 所有                             |
| fulfillmentType   | String   | NONE / LOCAL_RESERVE / TRANSFER_FULFILL | 所有                             |
| message           | String   | 详细提示信息                            | 所有                             |
| copyId            | Long     | 归还的副本ID                            | 所有                             |
| reservatorId      | Long     | 预约者ID（如有）                        | LOCAL_RESERVE / TRANSFER_FULFILL |
| reservationId     | Long     | 预约记录ID                              | LOCAL_RESERVE / TRANSFER_FULFILL |
| reserveExpireTime | DateTime | 预留过期时间（24小时后）                | LOCAL_RESERVE / TRANSFER_FULFILL |
| transferId        | Long     | 调拨单ID（跨馆时）                      | TRANSFER_FULFILL                 |
| creditPenalty     | Integer  | 扣除的信用分（0表示无逾期）             | 所有                             |
| nextAction        | String   | WAIT_FOR_PICKUP / TRANSPORTING / NONE   | 所有                             |

### 不同场景的响应示例

#### 场景A：无预约，直接还书

**条件**：该书目无PENDING预约
**响应示例**：

```json
{
  "success": true,
  "status": "SUCCESS",
  "message": "还书成功，书已回到馆1架上",
  "hasReservation": false,
  "reservatorId": null,
  "localReservation": false,
  "reservationId": null,
  "reserveExpireTime": null,
  "transferId": null
}
```

#### 场景B：就地预留

**条件**：用户A预约了该书，期望取书馆=还书馆（理科馆）
**响应示例**：

```json
{
  "success": true,
  "status": "SUCCESS",
  "message": "还书成功，书已直接预留给预约用户（24小时内有效）",
  "hasReservation": true,
  "reservatorId": 1,
  "localReservation": true,
  "reservationId": 9,
  "reserveExpireTime": "2026-02-27T19:37:47.7998726",
  "transferId": null
}
```

#### 场景C：跨馆调拨

**条件**：用户B预约了该书，期望取书馆=文科馆（id=2），还书馆=理科馆（id=1）
**响应示例**：

```json
{
  "success": true,
  "status": "SUCCESS",
  "message": "还书成功，已触发向预约用户指定馆的调拨（预计30分钟到达）",
  "hasReservation": true,
  "reservatorId": 1,
  "localReservation": false,
  "reservationId": 10,
  "reserveExpireTime": "2026-02-27T19:38:42.1529019",
  "transferId": 10
}
```

#### 失败场景：非借书人还书

**响应示例**：

```json
{
  "success": false,
  "status": "FAILED",
  "message": "非借书人本人，无法还书（当前借书人ID: 3）",
  "hasReservation": false
}
```

### 数据库变更

**基础还书（所有场景）**：

- **更新**：`tb_book_copy`（状态AVAILABLE，`library_id`改为还书馆，`last_borrow_time`更新）
- **更新**：`tb_book_borrow`（状态RETURNED，`return_time`写入）
- **更新**：`tb_user`（`current_borrow_count`-1，如逾期则`credit_score`-扣分，如信用分<60则`status`改为FROZEN）
- **更新**：`tb_library_biblio_stats`（还书馆`stock_count`+1）

**就地预留（场景B）**：

- **更新**：`tb_book_copy`（状态RESERVED）
- **更新**：`tb_book_reservation`（状态FULFILLED，`copy_id`写入，`fulfill_time`写入，`expire_time`=当前时间+24小时）
- **写入**：`tb_book_borrow`（状态RESERVED，`due_time`=当前时间+24小时）
- **更新**：`tb_user`（预约者`current_borrow_count`+1）

**跨馆调拨（场景C）**：

- **写入**：`tb_book_transfer`（状态IN_TRANSIT，`from_library_id`=还书馆，`to_library_id`=预约者馆）
- **更新**：`tb_book_copy`（状态IN_TRANSFER或保持RESERVED，`library_id`逻辑上属于目标馆但物理未到达）
- **更新**：`tb_book_reservation`（状态FULFILLED，`copy_id`写入，`fulfill_time`写入）
- **更新**：`tb_library_biblio_stats`（还书馆`stock_count`-1，目标馆暂不+1，等到货后+1）

**调拨到货回调（TransferAgent）**：

- **更新**：`tb_book_transfer`（状态COMPLETED，`complete_time`写入）
- **更新**：`tb_book_copy`（`library_id`改目标馆，状态RESERVED）
- **更新**：`tb_library_biblio_stats`（目标馆`stock_count`+1）
- **写入**：`tb_book_borrow`（状态RESERVED，预约者`current_borrow_count`+1，24小时过期）

**预留超期释放（ReservationMonitorAgent）**：

- **更新**：`tb_book_reservation`（状态EXPIRED）
- **更新**：`tb_book_copy`（状态AVAILABLE）
- **触发**：重新调用预约队列检查（同还书逻辑，查找下一个预约者）

### 测试场景矩阵

| 测试场景         | 初始数据设置                            | 预期策略                 | 关键验证点                                                   |
| :--------------- | :-------------------------------------- | :----------------------- | :----------------------------------------------------------- |
| 无预约还书       | tb_book_reservation无记录               | NONE                     | 副本状态AVAILABLE，库存+1，无借阅记录创建                    |
| 就地预留成功     | 馆1有预约，pickup_library_id=1          | LOCAL_RESERVE            | 副本状态RESERVED，预约状态FULFILLED，借阅记录状态RESERVED（过期时间24h后），预约者current_borrow_count+1 |
| 跨馆调拨触发     | 馆1还书，馆2有预约                      | TRANSFER_FULFILL         | tb_book_transfer有记录，副本状态IN_TRANSFER，馆1库存-1，馆2库存不变（到货后+1） |
| 调拨到货回调     | 存在IN_TRANSIT的调拨单                  | TRANSFER_COMPLETE        | 调拨单状态COMPLETED，副本移至馆2，馆2库存+1，创建RESERVED借阅记录，发送通知 |
| 预约者达上限跳过 | 队列：用户A（10/10本），用户B（2/10本） | LOCAL_RESERVE（给用户B） | 用户A预约状态CANCELED，用户B预约状态FULFILLED，副本RESERVED给用户B |
| 24小时预留超期   | 存在已过期（>24h）的RESERVED借阅记录    | RESERVE_EXPIRE           | 借阅记录状态EXPIRED，副本状态AVAILABLE，触发下一次预约检查（如有则分配给下一个） |
| 逾期还书扣分     | 应还时间为3天前                         | CREDIT_PENALTY           | 用户credit_score-6，如低于60则status改为FROZEN               |
| 重复还书         | 同一副本连续还两次                      | FAILED                   | 第二次返回"该书当前无借阅记录"                               |

------

## 功能3：调拨完成回调（Transfer Complete Callback）

### 功能描述

当图书通过物流从源馆运输到目标馆后，由物流人员或馆员确认收货，系统通过此接口完成调拨闭环。根据调拨类型（借书调拨 、还书调拨）采取不同策略：

- **借书调拨**：更新已有的运输中借阅记录为预留状态，启动24小时取书倒计时
- **还书调拨**：为预约用户创建新的预留借阅记录，启动24小时取书倒计时

### 参与智能体

| 智能体        | 角色               | 职责                                                  |
| :------------ | :----------------- | :---------------------------------------------------- |
| TransferAgent | Executor（执行者） | 验证调拨单、更新库存、创建/更新借阅记录、同步用户计数 |

### 业务流程（时序）

1. **接收回调**：TransferController接收调拨完成请求，校验参数合法性
2. **状态校验**：TransferService查询调拨单，确认状态为`IN_TRANSIT`（运输中）
3. **更新调拨单**：将调拨状态改为`COMPLETED`，记录实际到达时间
4. **实物上架**：更新图书副本物理位置到目标馆，状态改为`RESERVED`（已预留），设置默认位置为"馆X-调拨暂存区"
5. **库存同步**：目标馆库存统计+1（物理到货）
6. **策略分支**：
   - **分支A（借书调拨）**：查询`tb_book_borrow`中`TRANSFERRING`状态的记录，更新为`RESERVED`，设置24小时过期时间（用户计数已在发起时增加，此处不再增加）
   - **分支B（还书调拨）**：查询`tb_book_reservation`中`FULFILLED`状态的记录，创建新的`RESERVED`借阅记录，用户借阅计数+1
7. **结果返回**：返回创建的借阅记录ID和预留过期时间

### 核心算法

**调拨类型判定逻辑**：

```java
if (存在TRANSFERRING状态的Borrow记录) {
    //借书调拨：发起时创建了Borrow记录，只需更新状态
    更新Borrow为RESERVED;
} else {
    //还书调拨：发起时创建了Reservation，需新建Borrow并增加用户计数
    创建Borrow(RESERVED);
    用户计数+1;
}
```

### 输入参数

| 字段             | 类型     | 必填 | 说明                           | 示例                |
| :--------------- | :------- | :--- | :----------------------------- | :------------------ |
| transferId       | Long     | 是   | 调拨单ID                       | 20                  |
| actualArriveTime | DateTime | 否   | 实际到达时间（默认为当前时间） | 2026-02-26T22:30:00 |

**请求示例**：

```json
{
  "transferId": 20,
  "actualArriveTime": "2026-02-26T22:30:00"
}
```

### 输出结果（按策略区分）

| 字段              | 类型     | 说明                           | 适用策略 |
| :---------------- | :------- | :----------------------------- | :------- |
| success           | boolean  | 是否成功                       | 所有     |
| message           | String   | 详细提示信息                   | 所有     |
| borrowId          | Long     | 预留借阅记录ID（用户取书凭证） | 成功时   |
| reserveExpireTime | DateTime | 预留过期时间（24小时后）       | 成功时   |

### 不同场景的响应示例

#### 场景A：借书调拨完成（发起时已有Borrow记录）

**条件**：用户借书触发的调拨，运输到达目标馆
**响应示例**：

```json
{
  "success": true,
  "message": "调拨完成，图书已预留给用户5",
  "borrowId": 100,
  "reserveExpireTime": "2026-02-27T22:30:00"
}
```

#### 场景B：还书调拨完成（发起时无Borrow记录）

**条件**：用户还书触发的调拨，满足其他馆预约者
**响应示例**：

```json
{
  "success": true,
  "message": "调拨完成，图书已预留给用户8",
  "borrowId": 101,
  "reserveExpireTime": "2026-02-27T22:30:00"
}
```

#### 失败场景：调拨单不存在

**响应示例**：

```json
{
  "success": false,
  "message": "调拨单不存在",
  "borrowId": null,
  "reserveExpireTime": null
}
```

#### 失败场景：状态异常（非运输中）

**响应示例**：

```json
{
  "success": false,
  "message": "调拨单状态异常，当前状态：COMPLETED",
  "borrowId": null,
  "reserveExpireTime": null
}
```

### 数据库变更

**所有场景**：

- **更新**：`tb_book_transfer`（status→COMPLETED，complete_time写入）
- **更新**：`tb_book_copy`（library_id→目标馆，status→RESERVED，location→"馆X-调拨暂存区"）
- **更新**：`tb_library_biblio_stats`（目标馆stock_count+1）

**借书调拨场景（分支A）**：

- **更新**：`tb_book_borrow`（status→RESERVED，due_time→当前时间+24小时）

**还书调拨场景（分支B）**：

- **写入**：`tb_book_borrow`（status→RESERVED，due_time→当前时间+24小时）
- **更新**：`tb_user`（current_borrow_count+1）

### 测试场景矩阵

| 测试场景     | 前置条件                                                 | 预期策略   | 关键验证点                                                   |
| :----------- | :------------------------------------------------------- | :--------- | :----------------------------------------------------------- |
| 借书调拨完成 | 存在IN_TRANSIT调拨单，发起时创建了TRANSFERRING的Borrow   | 更新Borrow | 不新增Borrow记录，不增加用户计数，状态变为RESERVED，副本移至目标馆 |
| 还书调拨完成 | 存在IN_TRANSIT调拨单，发起时创建了FULFILLED的Reservation | 新建Borrow | 新增Borrow记录，用户计数+1，状态变为RESERVED，副本移至目标馆 |
| 重复完成     | 调拨单状态已为COMPLETED                                  | FAILED     | 返回"状态异常"，无数据库变更                                 |
| 调拨单不存在 | 传入不存在的transferId                                   | FAILED     | 返回"调拨单不存在"                                           |
