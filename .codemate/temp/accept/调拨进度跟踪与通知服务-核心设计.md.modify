# 调拨进度跟踪与通知服务 - 核心设计

**设计日期**: 2026年3月8日

---

## 一、调拨进度跟踪

### 1.1 核心功能

**提供3个查询接口**:
1. 查询单个调拨进度(根据transferId)
2. 查询批量调拨进度(根据orderId)
3. 查询用户的所有调拨记录

### 1.2 进度计算逻辑

**进度百分比计算**:
- PENDING: 0%
- IN_TRANSIT: 根据时间计算(已用时间/预计时间 * 100),最大95%
- COMPLETED: 100%
- CANCELED: 0%

**示例**: 预计6小时,已用3小时 → 进度50%

### 1.3 需要确认的问题

**问题1: 是否需要新增数据库字段?**

可选方案:
- **方案A(推荐)**: 不新增字段,使用现有字段计算进度
- **方案B**: 新增`progress_percentage`字段,定时更新

**问题2: 是否需要记录状态变更历史?**

可选方案:
- **方案A(推荐)**: 不记录,保持简单
- **方案B**: 新增`tb_transfer_status_history`表,记录每次状态变更

**问题3: 是否需要缓存进度信息?**

可选方案:
- **方案A(推荐)**: 使用Redis缓存,有效期5分钟
- **方案B**: 不缓存,每次实时计算

---

## 二、调拨通知服务

### 2.1 核心功能

**3种通知场景**:
1. 调拨发起通知(调拨创建时)
2. 调拨到达通知(调拨完成时)
3. 调拨延迟告警(超过预计时间未到达)

### 2.2 通知渠道

**优先级排序**:
1. **站内信**(必须实现) - 使用现有`tb_notification`表
2. 邮件(可选)
3. 短信(可选)

### 2.3 通知触发方式

**方案A(推荐): 事件驱动**
- 调拨业务发布事件
- 通知服务监听事件并发送通知
- 优点: 解耦,异步,不影响主流程

**方案B: 直接调用**
- 调拨业务直接调用通知服务
- 优点: 简单直接
- 缺点: 耦合,影响主流程性能

### 2.4 定时任务

**需要2个定时任务**:
1. **调拨延迟检测**: 每小时检查超时调拨,发送告警
2. **通知清理**: 每天清理30天前的已读通知

### 2.5 需要确认的问题

**问题1: 通知发送方式?**

可选方案:
- **方案A(推荐)**: 站内信同步发送,邮件/短信异步发送
- **方案B**: 所有通知都异步发送

**问题2: 是否使用事件驱动?**

可选方案:
- **方案A(推荐)**: 使用Spring Event,异步处理
- **方案B**: 直接调用通知服务

**问题3: 通知对象如何确定?**

需要确认:
- 用户请求调拨: 通知目标用户
- 库存平衡调拨: 通知谁?(目标馆管理员?系统管理员?)

---

## 三、实现优先级建议

**第一阶段(必须实现)**:
1. 调拨进度查询接口(3个)
2. 站内信通知(3种场景)
3. 调拨延迟检测定时任务

**第二阶段(可选实现)**:
1. 邮件/短信通知
2. 通知清理定时任务
3. 进度缓存优化

---

## 四、技术架构

**架构选择**: 三层架构 + 事件驱动

**不涉及Agent**: 纯业务逻辑,无需智能决策

**新增组件**:
- TransferProgressController: 进度查询接口
- TransferProgressService: 进度计算逻辑
- NotificationService: 通知发送服务
- TransferNotificationService: 调拨通知专用服务
- TransferEventListener: 事件监听器
- TransferDelayCheckTask: 延迟检测定时任务

---

## 五、需要您确认的决策

### 5.1 调拨进度跟踪

1. **是否新增`progress_percentage`字段?**
   - [ ] 新增字段,定时更新
   - [x] 不新增,实时计算(推荐)

2. **是否记录状态变更历史?**
   - [ ] 新增历史表
   - [x] 不记录(推荐)

3. **是否使用Redis缓存进度?**
   - [x] 使用缓存(推荐)
   - [ ] 不使用缓存

### 5.2 调拨通知服务

1. **通知发送方式?**
   - [x] 站内信同步,其他异步(推荐)
   - [ ] 全部异步

2. **是否使用事件驱动?**
   - [x] 使用Spring Event(推荐)
   - [ ] 直接调用

3. **库存平衡调拨通知谁?**
   - [ ] 目标馆管理员
   - [ ] 系统管理员
   - [ ] 不发送通知
   - [ ] 其他:________

4. **是否实现邮件/短信通知?**
   - [ ] 第一阶段就实现
   - [x] 第二阶段再实现(推荐)

---

## 六、下一步行动

确认以上决策后,我将:
1. 开始编码实现
2. 更新功能文档.md
3. 编写功能测试指导.md
4. 在项目改进记录.md中记录

**预计工作量**: 
- 调拨进度跟踪: 2-3小时
- 调拨通知服务: 3-4小时
- 总计: 5-7小时

---

**请确认以上设计方案,特别是第五部分的决策项。**
