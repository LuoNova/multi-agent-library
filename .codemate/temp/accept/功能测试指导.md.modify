# 功能测试指导文档

本文档详细说明如何测试多智能体图书馆资源调度系统的各项功能。

## 一、测试环境准备

### 1.1 数据库准备

在开始测试前，请先执行测试数据脚本：

```bash
# 在MySQL中执行测试数据脚本
mysql -u root -p library_db < test_data.sql
```

或者使用数据库管理工具（如Navicat、MySQL Workbench）执行`test_data.sql`文件。

### 1.2 启动应用

```bash
# 进入项目目录
cd D:/javaProjects/multi-agent-library

# 启动Spring Boot应用
mvn spring-boot:run
```

等待应用启动完成，看到以下日志表示启动成功：
```
Started MultiAgentLibraryApplication in X.XXX seconds
JADE Agents started successfully
```

### 1.3 访问Swagger文档

启动成功后，访问Swagger UI查看和测试API接口：
```
http://localhost:8080/swagger-ui.html
```

---

## 二、功能测试场景

### 功能1：智能借书与资源调度

#### 测试场景1.1：本地借阅（目标馆有库存）

**测试目的**：验证当目标馆有库存时，系统能够直接办理借阅。

**前置条件**：
- 用户ID=1（张三），状态正常，借阅计数为0
- 书目ID=1（《Java编程思想》）
- 理科馆（ID=1）有该书的可用副本

**测试步骤**：

1. 打开Swagger UI，找到`POST /api/borrow/request`接口
2. 输入以下请求参数：
```json
{
  "userId": 1,
  "biblioId": 1,
  "preferredLibraryId": 1
}
```
3. 点击"Execute"执行请求

**预期结果**：
- HTTP状态码：200
- 响应消息：`"借书成功，本地借阅"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `strategy`: "LOCAL_LOAN"
  - `borrowId`: 借阅记录ID（数字）
  - `copyId`: 副本ID（数字）

**验证方法**：
```sql
-- 查询借阅记录
SELECT * FROM tb_book_borrow WHERE user_id = 1 ORDER BY create_time DESC LIMIT 1;

-- 查询副本状态（应该变为BORROWED）
SELECT * FROM tb_book_copy WHERE id = <返回的copyId>;

-- 查询用户借阅计数（应该+1）
SELECT current_borrow_count FROM tb_user WHERE id = 1;
```

---

#### 测试场景1.2：跨馆调拨（目标馆无库存，其他馆有）

**测试目的**：验证当目标馆无库存但其他馆有库存时，系统能够自动触发跨馆调拨。

**前置条件**：
- 执行重置脚本，将理科馆的《Java编程思想》副本移至文科馆
```sql
-- 重置理科馆库存为0
UPDATE tb_book_copy SET library_id = 2 WHERE biblio_id = 1 AND library_id = 1;
UPDATE tb_library_biblio_stats SET stock_count = 0 WHERE library_id = 1 AND biblio_id = 1;
UPDATE tb_library_biblio_stats SET stock_count = 5 WHERE library_id = 2 AND biblio_id = 1;
```

**测试步骤**：

1. 使用Swagger调用借书接口：
```json
{
  "userId": 1,
  "biblioId": 1,
  "preferredLibraryId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"借书成功，已触发跨馆调拨"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `strategy`: "TRANSFER_PROVIDE"
  - `transferId`: 调拨单ID
  - `estimatedArrival`: 预计到达时间（30分钟后）

**验证方法**：
```sql
-- 查询调拨单（状态应为IN_TRANSIT）
SELECT * FROM tb_book_transfer WHERE id = <返回的transferId>;

-- 查询借阅记录（状态应为TRANSFERRING）
SELECT * FROM tb_book_borrow WHERE user_id = 1 ORDER BY create_time DESC LIMIT 1;

-- 查询副本状态（应为IN_TRANSIT）
SELECT * FROM tb_book_copy WHERE biblio_id = 1 AND status = 'IN_TRANSIT';
```

---

#### 测试场景1.3：预约排队（全馆无库存）

**测试目的**：验证当所有馆都无库存时，系统能够创建预约记录。

**前置条件**：
- 将所有《Java编程思想》副本设置为已借出状态
```sql
UPDATE tb_book_copy SET status = 'BORROWED' WHERE biblio_id = 1;
UPDATE tb_library_biblio_stats SET stock_count = 0 WHERE biblio_id = 1;
```

**测试步骤**：

1. 使用Swagger调用借书接口：
```json
{
  "userId": 1,
  "biblioId": 1,
  "preferredLibraryId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"借书成功，已加入预约队列"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `strategy`: "RESERVATION"
  - `reservationId`: 预约记录ID

**验证方法**：
```sql
-- 查询预约记录（状态应为PENDING）
SELECT * FROM tb_book_reservation WHERE user_id = 1 AND biblio_id = 1;
```

---

### 功能2：调拨完成确认

#### 测试场景2.1：借书调拨完成

**测试目的**：验证调拨到达后，系统能够正确完成调拨并创建预留记录。

**前置条件**：
- 已完成测试场景1.2（跨馆调拨）
- 调拨单状态为IN_TRANSIT

**测试步骤**：

1. 使用Swagger调用调拨完成接口：
```
POST /api/transfer/complete
```
请求参数：
```json
{
  "transferId": <场景1.2返回的transferId>
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"调拨完成，已为用户预留24小时"`
- 响应数据包含：
  - `borrowId`: 借阅记录ID
  - `pickupDeadline`: 取书截止时间（24小时后）

**验证方法**：
```sql
-- 查询调拨单状态（应为COMPLETED）
SELECT * FROM tb_book_transfer WHERE id = <transferId>;

-- 查询借阅记录状态（应为RESERVED）
SELECT * FROM tb_book_borrow WHERE id = <borrowId>;

-- 查询副本状态（应为RESERVED，位置在目标馆）
SELECT * FROM tb_book_copy WHERE id = <copyId>;

-- 查询目标馆库存（应该+1）
SELECT stock_count FROM tb_library_biblio_stats WHERE library_id = 1 AND biblio_id = 1;
```

---

### 功能3：取书确认

#### 测试场景3.1：正常取书

**测试目的**：验证用户在预留期内取书，系统能够正确更新状态。

**前置条件**：
- 已完成测试场景2.1（调拨完成）
- 借阅记录状态为RESERVED
- 当前时间在取书截止时间前

**测试步骤**：

1. 使用Swagger调用取书确认接口：
```
POST /api/pickup/confirm
```
请求参数：
```json
{
  "borrowId": <场景2.1返回的borrowId>,
  "userId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：取书成功
- 响应数据包含：
  - `borrowId`: 借阅记录ID
  - `bookTitle`: "Java编程思想"
  - `borrowDate`: 借阅日期
  - `dueDate`: 应还日期（30天后）

**验证方法**：
```sql
-- 查询借阅记录状态（应为BORROWING）
SELECT * FROM tb_book_borrow WHERE id = <borrowId>;

-- 查询副本状态（应为BORROWED）
SELECT * FROM tb_book_copy WHERE id = <copyId>;

-- 查询实际取书时间
SELECT actual_pickup_time FROM tb_book_borrow WHERE id = <borrowId>;
```

---

#### 测试场景3.2：非预约人取书（异常测试）

**测试目的**：验证系统拒绝非预约人取书。

**前置条件**：
- 借阅记录状态为RESERVED
- 预约人ID=1

**测试步骤**：

1. 使用其他用户ID（如ID=2）尝试取书：
```json
{
  "borrowId": <borrowId>,
  "userId": 2
}
```

**预期结果**：
- HTTP状态码：400
- 响应消息：`"仅限预约人本人取书"`

---

#### 测试场景3.3：超期取书（异常测试）

**测试目的**：验证系统拒绝超期取书。

**前置条件**：
- 借阅记录状态为RESERVED
- 手动将取书截止时间设置为过去时间：
```sql
UPDATE tb_book_borrow SET pickup_deadline = NOW() - INTERVAL 1 HOUR WHERE id = <borrowId>;
```

**测试步骤**：

1. 使用Swagger调用取书确认接口：
```json
{
  "borrowId": <borrowId>,
  "userId": 1
}
```

**预期结果**：
- HTTP状态码：400
- 响应消息：`"预留已超期,请重新预约"`

---

### 功能4：还书与预约自动兑现

#### 测试场景4.1：还书且无预约

**测试目的**：验证正常还书流程。

**前置条件**：
- 用户ID=1有一本借阅中的书
- 该书无预约记录

**测试步骤**：

1. 使用Swagger调用还书接口：
```
POST /api/return/process
```
请求参数：
```json
{
  "copyId": <借阅的副本ID>,
  "userId": 1,
  "returnLibraryId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"还书成功"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `hasReservation`: false

**验证方法**：
```sql
-- 查询借阅记录状态（应为RETURNED）
SELECT * FROM tb_book_borrow WHERE copy_id = <copyId> AND user_id = 1;

-- 查询副本状态（应为AVAILABLE）
SELECT * FROM tb_book_copy WHERE id = <copyId>;

-- 查询用户借阅计数（应该-1）
SELECT current_borrow_count FROM tb_user WHERE id = 1;

-- 查询还书馆库存（应该+1）
SELECT stock_count FROM tb_library_biblio_stats WHERE library_id = 1 AND biblio_id = <biblioId>;
```

---

#### 测试场景4.2：还书触发预约兑现（同馆）

**测试目的**：验证还书时自动分配给预约者。

**前置条件**：
- 用户ID=2借了《Java编程思想》
- 用户ID=1预约了该书，希望在理科馆取书
- 用户ID=2在理科馆还书

**准备数据**：
```sql
-- 执行重置脚本
-- 参考"重置数据库中书籍状态.md"中的场景1
```

**测试步骤**：

1. 使用Swagger调用还书接口：
```json
{
  "copyId": 1001,
  "userId": 2,
  "returnLibraryId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"还书成功，已为预约用户预留"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `hasReservation`: true
  - `reservatorId`: 1
  - `localReservation`: true
  - `reservationId`: 预约记录ID

**验证方法**：
```sql
-- 查询预约记录状态（应为FULFILLED）
SELECT * FROM tb_book_reservation WHERE user_id = 1 AND biblio_id = 1;

-- 查询借阅记录（应为RESERVED状态，用户ID=1）
SELECT * FROM tb_book_borrow WHERE copy_id = 1001 AND user_id = 1;

-- 查询副本状态（应为RESERVED）
SELECT * FROM tb_book_copy WHERE id = 1001;
```

---

#### 测试场景4.3：还书触发跨馆调拨

**测试目的**：验证还书时自动触发向预约者指定馆的调拨。

**前置条件**：
- 用户ID=2在理科馆借了《Java编程思想》
- 用户ID=1预约了该书，希望在文科馆取书
- 用户ID=2在理科馆还书

**准备数据**：
```sql
-- 执行重置脚本
-- 参考"重置数据库中书籍状态.md"中的场景2
```

**测试步骤**：

1. 使用Swagger调用还书接口：
```json
{
  "copyId": 1001,
  "userId": 2,
  "returnLibraryId": 1
}
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"还书成功，已触发向预约用户指定馆的调拨"`
- 响应数据包含：
  - `status`: "SUCCESS"
  - `hasReservation`: true
  - `reservatorId`: 1
  - `localReservation`: false
  - `transferId`: 调拨单ID

**验证方法**：
```sql
-- 查询调拨单（状态应为IN_TRANSIT）
SELECT * FROM tb_book_transfer WHERE id = <transferId>;

-- 查询预约记录状态（应为FULFILLED）
SELECT * FROM tb_book_reservation WHERE user_id = 1 AND biblio_id = 1;

-- 查询副本状态（应为IN_TRANSIT）
SELECT * FROM tb_book_copy WHERE id = 1001;
```

---

#### 测试场景4.4：逾期还书扣分

**测试目的**：验证逾期还书时自动扣除信用分。

**前置条件**：
- 用户ID=1借了一本书，已逾期5天

**准备数据**：
```sql
-- 创建逾期借阅记录
UPDATE tb_book_borrow
SET due_time = NOW() - INTERVAL 5 DAY
WHERE user_id = 1 AND status = 'BORROWING';
```

**测试步骤**：

1. 使用Swagger调用还书接口

**预期结果**：
- HTTP状态码：200
- 响应消息包含逾期提示
- 用户信用分扣除10分（5天 × 2分/天）

**验证方法**：
```sql
-- 查询用户信用分（应为90）
SELECT credit_score FROM tb_user WHERE id = 1;
```

---

### 功能5：预约过期自动释放

#### 测试场景5.1：预留超期自动释放

**测试目的**：验证定时任务自动释放超期未取的预留记录。

**前置条件**：
- 借阅记录状态为RESERVED
- 取书截止时间已过

**准备数据**：
```sql
-- 手动设置取书截止时间为过去
UPDATE tb_book_borrow
SET pickup_deadline = NOW() - INTERVAL 1 HOUR
WHERE status = 'RESERVED';
```

**测试步骤**：

1. 等待定时任务执行（每小时执行一次）
2. 或手动触发定时任务（开发环境）

**预期结果**：
- 借阅记录状态变为CANCELLED
- 副本状态变为AVAILABLE
- 用户信用分扣除5分
- 发送超期通知

**验证方法**：
```sql
-- 查询借阅记录状态
SELECT status FROM tb_book_borrow WHERE id = <borrowId>;

-- 查询副本状态
SELECT status FROM tb_book_copy WHERE id = <copyId>;

-- 查询用户信用分
SELECT credit_score FROM tb_user WHERE id = <userId>;

-- 查询通知记录
SELECT * FROM tb_notification WHERE user_id = <userId> AND type = 'RESERVE_EXPIRED';
```

---

## 三、异常场景测试

### 3.1 用户已达借阅上限

**测试目的**：验证系统拒绝超过借阅上限的用户借书。

**前置条件**：
- 用户当前借阅数已达上限（10本）

**准备数据**：
```sql
UPDATE tb_user SET current_borrow_count = 10 WHERE id = 1;
```

**测试步骤**：

1. 使用Swagger调用借书接口

**预期结果**：
- HTTP状态码：200
- 响应消息：`"已达借阅上限"`
- `status`: "FAILED"

---

### 3.2 用户被冻结

**测试目的**：验证系统拒绝被冻结用户借书。

**前置条件**：
- 用户状态为FROZEN

**准备数据**：
```sql
UPDATE tb_user SET status = 'FROZEN' WHERE id = 1;
```

**测试步骤**：

1. 使用Swagger调用借书接口

**预期结果**：
- HTTP状态码：200
- 响应消息：`"用户账号已被冻结"`
- `status`: "FAILED"

---

### 3.3 重复借书

**测试目的**：验证系统拒绝用户重复借阅同一本书。

**前置条件**：
- 用户已借阅该书（状态为BORROWING、RESERVED或TRANSFERRING）

**测试步骤**：

1. 使用Swagger调用借书接口，借阅同一本书

**预期结果**：
- HTTP状态码：200
- 响应消息：`"您已借阅该书（可能在调拨中）"`
- `status`: "FAILED"

---

### 3.4 非借书人还书

**测试目的**：验证系统拒绝非借书人还书。

**前置条件**：
- 书被用户ID=1借走
- 用户ID=2尝试还书

**测试步骤**：

1. 使用Swagger调用还书接口，userId=2

**预期结果**：
- HTTP状态码：200
- 响应消息：`"非借书人本人，无法还书"`
- `status`: "FAILED"

---

## 四、性能测试建议

### 4.1 并发借书测试

使用JMeter或Postman进行并发测试：
- 模拟100个用户同时借书
- 验证库存扣减的正确性
- 验证不会出现超卖现象

### 4.2 多智能体协商性能测试

- 测试多智能体协商的响应时间
- 建议平均响应时间 < 3秒
- 测试高并发场景下的稳定性

---

## 五、测试数据重置

每次测试完成后，建议重置数据库：

```bash
mysql -u root -p library_db < test_data.sql
```

或使用项目提供的重置脚本：
```bash
mysql -u root -p library_db < 重置数据库中书籍状态.md
```

---

## 六、常见问题排查

### 6.1 借书请求超时

**可能原因**：
- 多智能体协商时间过长
- 数据库连接超时
- JADE Agent未正常启动

**排查方法**：
1. 检查JADE Agent是否正常启动（查看日志）
2. 检查数据库连接是否正常
3. 查看BorrowController中的超时设置（默认5秒）

### 6.2 调拨单无法完成

**可能原因**：
- 调拨单状态不是IN_TRANSIT
- 关联的借阅记录或预约记录不存在

**排查方法**：
```sql
-- 查询调拨单状态
SELECT * FROM tb_book_transfer WHERE id = <transferId>;

-- 查询关联的借阅记录
SELECT * FROM tb_book_borrow WHERE copy_id = <copyId> AND status = 'TRANSFERRING';

-- 查询关联的预约记录
SELECT * FROM tb_book_reservation WHERE copy_id = <copyId> AND status = 'FULFILLED';
```

### 6.3 取书确认失败

**可能原因**：
- 借阅记录状态不是RESERVED
- 已超过取书截止时间
- 用户ID不匹配

**排查方法**：
```sql
-- 查询借阅记录详情
SELECT * FROM tb_book_borrow WHERE id = <borrowId>;

-- 检查取书截止时间
SELECT pickup_deadline FROM tb_book_borrow WHERE id = <borrowId>;
```

---

## 七、主动调拨功能测试

### 功能5：主动调拨与库存平衡

#### 测试场景5.1：手动触发库存平衡分析

**测试目的**：验证系统能够正确分析库存并生成调拨建议。

**前置条件**：
- 数据库中存在库存不均衡的情况
- 例如：理科馆某书库存不足，文科馆该库存充足

**准备数据**：
```sql
-- 设置理科馆《Java编程思想》库存不足且需求旺盛
UPDATE tb_library_biblio_stats 
SET stock_count = 1, borrow_count_30d = 25, reservation_pending_count = 5
WHERE library_id = 1 AND biblio_id = 1;

-- 设置文科馆《Java编程思想》库存充足且利用率低
UPDATE tb_library_biblio_stats 
SET stock_count = 8, borrow_count_30d = 5, reservation_pending_count = 0
WHERE library_id = 2 AND biblio_id = 1;

-- 确保文科馆有可用的副本
UPDATE tb_book_copy 
SET library_id = 2, status = 'AVAILABLE'
WHERE biblio_id = 1 AND library_id = 2 LIMIT 5;
```

**测试步骤**：

1. 使用Swagger调用手动触发接口：
```
POST /api/transfer/trigger-balance
```

**预期结果**：
- HTTP状态码：200
- 响应消息：`"库存平衡分析已触发"`
- 控制台打印分析日志
- 数据库中生成调拨建议记录

**验证方法**：
```sql
-- 查询调拨建议
SELECT * FROM tb_transfer_suggestion WHERE status = 'PENDING' OR status = 'EXECUTED';

-- 如果调拨数量≤3，应该自动执行
SELECT * FROM tb_book_transfer WHERE transfer_reason = 'INVENTORY_BALANCE';
```

---

#### 测试场景5.2：自动审批执行（小批量调拨）

**测试目的**：验证调拨数量≤3本时自动审批并执行。

**前置条件**：
- 已生成调拨建议
- 调拨数量≤3本

**测试步骤**：

1. 触发库存平衡分析
2. 查看控制台日志，确认自动审批执行

**预期结果**：
- 调拨建议状态：EXECUTED
- 调拨单已创建
- 副本状态已更新为IN_TRANSIT

**验证方法**：
```sql
-- 查询调拨建议状态
SELECT id, status, suggested_quantity FROM tb_transfer_suggestion ORDER BY create_time DESC LIMIT 1;

-- 查询调拨单
SELECT * FROM tb_book_transfer WHERE transfer_reason = 'INVENTORY_BALANCE' ORDER BY request_time DESC LIMIT 1;

-- 查询副本状态
SELECT id, status, library_id FROM tb_book_copy WHERE biblio_id = 1 AND status = 'IN_TRANSIT';
```

---

#### 测试场景5.3：人工审批执行（大批量调拨）

**测试目的**：验证调拨数量>3本时等待人工审批。

**前置条件**：
- 调拨数量>3本

**准备数据**：
```sql
-- 设置理科馆需求缺口>3本
UPDATE tb_library_biblio_stats 
SET stock_count = 0, borrow_count_30d = 30, reservation_pending_count = 10
WHERE library_id = 1 AND biblio_id = 1;

-- 设置文科馆库存充足
UPDATE tb_library_biblio_stats 
SET stock_count = 30, borrow_count_30d = 3, reservation_pending_count = 0
WHERE library_id = 2 AND biblio_id = 1;
```

**测试步骤**：

1. 触发库存平衡分析
2. 查看控制台，确认打印了待审批通知
3. 使用Swagger查询待审批建议：
```
GET /api/transfer/suggestion/list
```
4. 审批通过：
```
POST /api/transfer/suggestion/approve/{id}?approverId=1
```

**预期结果**：
- 控制台打印待审批通知
- 建议状态：PENDING
- 审批后状态：APPROVED → EXECUTED
- 调拨单已创建

**验证方法**：
```sql
-- 查询建议状态变化
SELECT id, status, approver_id, approve_time FROM tb_transfer_suggestion WHERE id = <suggestionId>;

-- 查询调拨单
SELECT * FROM tb_book_transfer WHERE suggestion_id = <suggestionId>;
```

---

#### 测试场景5.4：审批拒绝

**测试目的**：验证拒绝调拨建议后不创建调拨单。

**前置条件**：
- 存在PENDING状态的调拨建议

**测试步骤**：

1. 使用Swagger拒绝建议：
```
POST /api/transfer/suggestion/reject/{id}?approverId=1&reason=测试拒绝
```

**预期结果**：
- HTTP状态码：200
- 建议状态：REJECTED
- 不创建调拨单

**验证方法**：
```sql
-- 查询建议状态
SELECT id, status, reason FROM tb_transfer_suggestion WHERE id = <suggestionId>;

-- 确认没有调拨单
SELECT COUNT(*) FROM tb_book_transfer WHERE suggestion_id = <suggestionId>;
```

---

#### 测试场景5.5：定时任务执行

**测试目的**：验证定时任务能够自动执行库存平衡分析。

**前置条件**：
- 应用正在运行
- 数据库中存在库存不均衡的情况

**测试步骤**：

1. 等待定时任务执行（每天凌晨2点）
2. 或修改定时任务时间进行测试

**预期结果**：
- 控制台打印定时任务执行日志
- 生成调拨建议并执行

**验证方法**：
```sql
-- 查询今天生成的调拨建议
SELECT * FROM tb_transfer_suggestion 
WHERE DATE(create_time) = CURDATE();
```

---

#### 测试场景5.6：无需调拨

**测试目的**：验证所有馆库存均衡时不生成调拨建议。

**前置条件**：
- 所有馆库存均衡

**准备数据**：
```sql
-- 设置所有馆库存均衡
UPDATE tb_library_biblio_stats 
SET stock_count = 5, borrow_count_30d = 10, reservation_pending_count = 0
WHERE biblio_id = 1;
```

**测试步骤**：

1. 触发库存平衡分析

**预期结果**：
- 控制台打印"未发现需要调拨的书籍"
- 不生成调拨建议

**验证方法**：
```sql
-- 查询今天生成的调拨建议（应该为空）
SELECT * FROM tb_transfer_suggestion 
WHERE DATE(create_time) = CURDATE();
```

---

### 异常场景测试

#### 测试场景5.7：源馆无可用副本

**测试目的**：验证源馆无可用副本时调拨失败。

**前置条件**：
- 源馆库存充足但所有副本都被借出

**准备数据**：
```sql
-- 设置文科馆库存充足但副本都被借出
UPDATE tb_library_biblio_stats 
SET stock_count = 8, borrow_count_30d = 5
WHERE library_id = 2 AND biblio_id = 1;

UPDATE tb_book_copy 
SET status = 'BORROWED'
WHERE biblio_id = 1 AND library_id = 2;
```

**测试步骤**：

1. 触发库存平衡分析

**预期结果**：
- 调拨建议状态：REJECTED
- 原因：源馆没有可用的副本

**验证方法**：
```sql
-- 查询建议状态
SELECT id, status, reason FROM tb_transfer_suggestion ORDER BY create_time DESC LIMIT 1;
```

---

## 九、主动调拨功能测试报告模板

| 测试场景 | 测试结果 | 备注 |
|---------|---------|------|
| 场景5.1：手动触发分析 | ✅ 通过 / ❌ 失败 | |
| 场景5.2：自动审批执行 | ✅ 通过 / ❌ 失败 | |
| 场景5.3：人工审批执行 | ✅ 通过 / ❌ 失败 | |
| 场景5.4：审批拒绝 | ✅ 通过 / ❌ 失败 | |
| 场景5.5：定时任务执行 | ✅ 通过 / ❌ 失败 | |
| 场景5.6：无需调拨 | ✅ 通过 / ❌ 失败 | |
| 场景5.7：源馆无可用副本 | ✅ 通过 / ❌ 失败 | |

---

**测试完成日期**：____年__月__日

**测试人员**：__________
